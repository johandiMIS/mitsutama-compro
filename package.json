{
  "name": "compro",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm --filter \"./apps/*\" --parallel run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build:wsl": "wsl bash scripts/wsl-turbo.sh build",
    "lint:wsl": "wsl bash scripts/wsl-turbo.sh lint",
    "test:wsl": "wsl bash scripts/wsl-turbo.sh test"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=20"
  }
}
